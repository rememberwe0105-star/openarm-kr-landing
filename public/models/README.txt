이 폴더에 OpenArm 2.0 3D 모델 파일을 넣어주세요.

- 파일명: openarm-2.glb  (반드시 이 이름)
- 형식: glTF Binary (.glb) 또는 .gltf
- 페이지(/v2)의 3D 뷰어가 자동으로 /models/openarm-2.glb 를 불러옵니다.
- 파일이 없으면 뷰어는 "3D 모델 준비 중" 안내를 표시합니다(에러 없음).

CAD(STEP/STP/SLDPRT) 또는 STL/OBJ 파일만 있는 경우 GLB로 변환이 필요합니다.
(Blender: File > Import > 해당 형식 → File > Export > glTF Binary(.glb))
